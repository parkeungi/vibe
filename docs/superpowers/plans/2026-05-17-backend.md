# 바이브 스토어 백엔드 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Next.js App Router Route Handlers로 바이브 스토어의 인증·상품·결제·파일 다운로드·관리자 API를 구현한다.

**Architecture:** Supabase가 DB(PostgreSQL)·Auth·Storage를 담당하며, Next.js Route Handlers가 API 레이어 역할을 한다. Portone V2 REST API로 결제를 검증하고, Resend로 구매 확인 이메일을 발송한다.

**Tech Stack:** Next.js 15 (App Router), TypeScript, Supabase (@supabase/ssr), Portone V2 (fetch), Resend

---

## 파일 구조 (생성/수정 목록)

```
/
├── app/
│   ├── api/
│   │   ├── auth/me/route.ts
│   │   ├── products/
│   │   │   ├── route.ts
│   │   │   └── [id]/
│   │   │       ├── route.ts
│   │   │       └── download/route.ts
│   │   ├── payments/verify/route.ts
│   │   ├── orders/my/route.ts
│   │   └── admin/
│   │       ├── products/
│   │       │   ├── route.ts
│   │       │   └── [id]/route.ts
│   │       └── dashboard/route.ts
│   └── layout.tsx
├── lib/
│   ├── supabase/
│   │   ├── server.ts
│   │   └── client.ts
│   ├── portone.ts
│   └── email.ts
├── types/index.ts
├── middleware.ts
├── supabase/migrations/
│   ├── 001_create_tables.sql
│   └── 002_rls_policies.sql
└── .env.local  (이미 존재, 값 추가)
```

---

## Task 1: Next.js 프로젝트 초기화

**Files:**
- Create: `app/layout.tsx`, `next.config.ts`, `package.json`, `tsconfig.json`, `tailwind.config.ts`, `postcss.config.mjs`

- [ ] **Step 1: 프로젝트 초기화**

```powershell
# C:\Users\parkeungi\Desktop\vibe 에서 실행
# 기존 README.md 임시 이동 후 초기화 (create-next-app이 덮어쓰므로)
Move-Item README.md README.md.bak -ErrorAction SilentlyContinue
npx create-next-app@latest . --typescript --tailwind --eslint --app --no-git --no-src-dir --import-alias "@/*" --yes
```

Expected output: `Success! Created ... at ...`

- [ ] **Step 2: README 복원**

```powershell
# create-next-app이 생성한 README.md를 프로젝트용으로 교체
Move-Item README.md.bak README.md -Force -ErrorAction SilentlyContinue
```

- [ ] **Step 3: app/layout.tsx를 최소한으로 설정**

```typescript
// app/layout.tsx
import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: '바이브 스토어',
  description: '나만의 지식 상품 커머스 플랫폼',
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ko">
      <body>{children}</body>
    </html>
  )
}
```

- [ ] **Step 4: 개발 서버 기동 확인**

```powershell
npm run dev
```

Expected: `Ready - started server on 0.0.0.0:3000`  
`Ctrl+C`로 종료

- [ ] **Step 5: 커밋**

```powershell
git add app/ next.config.ts package.json tsconfig.json tailwind.config.ts postcss.config.mjs .eslintrc.json public/
git commit -m "feat: initialize Next.js App Router project"
```

---

## Task 2: 의존성 설치 및 환경변수 설정

**Files:**
- Modify: `package.json`, `.env.local`

- [ ] **Step 1: Supabase SSR 패키지 설치**

```powershell
npm install @supabase/ssr @supabase/supabase-js
```

Expected: `added ... packages`

- [ ] **Step 2: Resend 설치**

```powershell
npm install resend
```

- [ ] **Step 3: .env.local에 Supabase 앱 키 추가**

Supabase 대시보드 → Project Settings → API 에서 값 복사 후 `.env.local` 업데이트:

```env
# 기존 값 유지 + 아래 추가
NEXT_PUBLIC_SUPABASE_URL=https://qisbzpglczxzfaaaoxxw.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<대시보드에서 복사한 anon key>
SUPABASE_SERVICE_ROLE_KEY=<대시보드에서 복사한 service_role key>

PORTONE_API_SECRET=<포트원 V2 API 시크릿>
PORTONE_CHANNEL_KEY=<포트원 채널 키>

RESEND_API_KEY=<Resend API 키>
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

- [ ] **Step 4: 커밋**

```powershell
git add package.json package-lock.json
git commit -m "feat: install supabase-ssr and resend packages"
```

---

## Task 3: TypeScript 타입 정의

**Files:**
- Create: `types/index.ts`

- [ ] **Step 1: types/index.ts 생성**

```typescript
// types/index.ts
export type Role = 'user' | 'admin'
export type OrderStatus = 'pending' | 'paid' | 'failed'

export interface Profile {
  id: string
  email: string
  name: string | null
  avatar_url: string | null
  role: Role
  created_at: string
}

export interface Product {
  id: string
  title: string
  description: string | null
  content: string | null
  thumbnail_url: string | null
  file_path: string | null
  file_name: string | null
  price: number
  is_active: boolean
  created_at: string
}

export interface ProductListItem {
  id: string
  title: string
  thumbnail_url: string | null
  price: number
  rating: number
  reviewCount: number
}

export interface ProductDetail extends Product {
  rating: number
  reviewCount: number
  reviews: ReviewWithProfile[]
}

export interface Order {
  id: string
  user_id: string
  product_id: string
  status: OrderStatus
  payment_id: string | null
  amount: number
  created_at: string
}

export interface OrderWithProduct extends Order {
  products: Pick<Product, 'id' | 'title' | 'thumbnail_url' | 'file_name'>
}

export interface Review {
  id: string
  user_id: string
  product_id: string
  order_id: string
  rating: number
  content: string | null
  created_at: string
}

export interface ReviewWithProfile extends Review {
  profiles: Pick<Profile, 'name' | 'avatar_url'>
}

export interface DashboardStats {
  totalSales: number
  todaySalesCount: number
  salesChart: Record<string, number>
}
```

- [ ] **Step 2: 타입 체크**

```powershell
npx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 3: 커밋**

```powershell
git add types/
git commit -m "feat: add TypeScript type definitions"
```

---

## Task 4: Supabase DB 마이그레이션 — 테이블 생성

**Files:**
- Create: `supabase/migrations/001_create_tables.sql`

- [ ] **Step 1: 마이그레이션 파일 생성**

```sql
-- supabase/migrations/001_create_tables.sql
CREATE TABLE IF NOT EXISTS profiles (
  id uuid REFERENCES auth.users(id) ON DELETE CASCADE PRIMARY KEY,
  email text NOT NULL,
  name text,
  avatar_url text,
  role text NOT NULL DEFAULT 'user' CHECK (role IN ('user', 'admin')),
  created_at timestamptz DEFAULT now()
);

CREATE TABLE IF NOT EXISTS products (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  title text NOT NULL,
  description text,
  content text,
  thumbnail_url text,
  file_path text,
  file_name text,
  price integer NOT NULL CHECK (price >= 0),
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamptz DEFAULT now()
);

CREATE TABLE IF NOT EXISTS orders (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id uuid REFERENCES profiles(id) ON DELETE CASCADE NOT NULL,
  product_id uuid REFERENCES products(id) NOT NULL,
  status text NOT NULL DEFAULT 'pending' CHECK (status IN ('pending', 'paid', 'failed')),
  payment_id text,
  amount integer NOT NULL CHECK (amount >= 0),
  created_at timestamptz DEFAULT now()
);

CREATE TABLE IF NOT EXISTS reviews (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id uuid REFERENCES profiles(id) ON DELETE CASCADE NOT NULL,
  product_id uuid REFERENCES products(id) NOT NULL,
  order_id uuid REFERENCES orders(id) NOT NULL UNIQUE,
  rating integer NOT NULL CHECK (rating BETWEEN 1 AND 5),
  content text,
  created_at timestamptz DEFAULT now()
);

CREATE OR REPLACE FUNCTION handle_new_user()
RETURNS trigger AS $$
BEGIN
  INSERT INTO profiles (id, email, name, avatar_url)
  VALUES (
    NEW.id,
    NEW.email,
    NEW.raw_user_meta_data->>'full_name',
    NEW.raw_user_meta_data->>'avatar_url'
  )
  ON CONFLICT (id) DO NOTHING;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

DROP TRIGGER IF EXISTS on_auth_user_created ON auth.users;
CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION handle_new_user();
```

- [ ] **Step 2: Supabase MCP로 마이그레이션 실행**

`mcp__claude_ai_Supabase__apply_migration` 도구 사용:
- project_id: `qisbzpglczxzfaaaoxxw`
- name: `create_tables`
- query: (001_create_tables.sql 전체 내용)

Expected: 성공 응답, 에러 없음

- [ ] **Step 3: 테이블 생성 확인**

`mcp__claude_ai_Supabase__list_tables` 도구 사용:
- project_id: `qisbzpglczxzfaaaoxxw`

Expected: `profiles`, `products`, `orders`, `reviews` 4개 테이블 존재

- [ ] **Step 4: 커밋**

```powershell
git add supabase/
git commit -m "feat: add DB migration - create tables"
```

---

## Task 5: Supabase RLS 정책 설정

**Files:**
- Create: `supabase/migrations/002_rls_policies.sql`

- [ ] **Step 1: RLS 마이그레이션 파일 생성**

```sql
-- supabase/migrations/002_rls_policies.sql
ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;
ALTER TABLE products ENABLE ROW LEVEL SECURITY;
ALTER TABLE orders ENABLE ROW LEVEL SECURITY;
ALTER TABLE reviews ENABLE ROW LEVEL SECURITY;

-- profiles
CREATE POLICY "profiles_select_own" ON profiles FOR SELECT USING (auth.uid() = id);
CREATE POLICY "profiles_insert_own" ON profiles FOR INSERT WITH CHECK (auth.uid() = id);
CREATE POLICY "profiles_update_own" ON profiles FOR UPDATE USING (auth.uid() = id);

-- products: 활성 상품 전체 조회, 관리자만 CUD
CREATE POLICY "products_select_active" ON products FOR SELECT USING (is_active = true);
CREATE POLICY "products_admin_insert" ON products FOR INSERT
  WITH CHECK (EXISTS (SELECT 1 FROM profiles WHERE id = auth.uid() AND role = 'admin'));
CREATE POLICY "products_admin_update" ON products FOR UPDATE
  USING (EXISTS (SELECT 1 FROM profiles WHERE id = auth.uid() AND role = 'admin'));
CREATE POLICY "products_admin_delete" ON products FOR DELETE
  USING (EXISTS (SELECT 1 FROM profiles WHERE id = auth.uid() AND role = 'admin'));

-- orders: 본인만 SELECT/INSERT, UPDATE는 서버(service_role)만
CREATE POLICY "orders_select_own" ON orders FOR SELECT USING (auth.uid() = user_id);
CREATE POLICY "orders_insert_own" ON orders FOR INSERT WITH CHECK (auth.uid() = user_id);

-- reviews: 전체 조회, paid order 보유자만 INSERT, 본인만 UPDATE/DELETE
CREATE POLICY "reviews_select_all" ON reviews FOR SELECT USING (true);
CREATE POLICY "reviews_insert_buyer" ON reviews FOR INSERT
  WITH CHECK (
    EXISTS (
      SELECT 1 FROM orders
      WHERE orders.id = order_id
        AND orders.user_id = auth.uid()
        AND orders.status = 'paid'
    )
  );
CREATE POLICY "reviews_update_own" ON reviews FOR UPDATE USING (auth.uid() = user_id);
CREATE POLICY "reviews_delete_own" ON reviews FOR DELETE USING (auth.uid() = user_id);
```

- [ ] **Step 2: Supabase MCP로 RLS 마이그레이션 실행**

`mcp__claude_ai_Supabase__apply_migration` 도구 사용:
- project_id: `qisbzpglczxzfaaaoxxw`
- name: `rls_policies`
- query: (002_rls_policies.sql 전체 내용)

Expected: 성공 응답

- [ ] **Step 3: 커밋**

```powershell
git add supabase/migrations/002_rls_policies.sql
git commit -m "feat: apply RLS policies to all tables"
```

---

## Task 6: Supabase Storage 버킷 설정

**Files:**
- (Supabase MCP로 직접 설정)

- [ ] **Step 1: 비공개 Storage 버킷 생성**

`mcp__claude_ai_Supabase__execute_sql` 도구 사용:
- project_id: `qisbzpglczxzfaaaoxxw`
- query:

```sql
INSERT INTO storage.buckets (id, name, public, file_size_limit, allowed_mime_types)
VALUES (
  'product-files',
  'product-files',
  false,
  52428800,
  ARRAY['application/pdf', 'application/zip', 'application/x-zip-compressed']
)
ON CONFLICT (id) DO NOTHING;
```

Expected: 성공

- [ ] **Step 2: Storage RLS 정책 — 서버(service_role)만 업로드 가능, 다운로드는 API 경유**

`mcp__claude_ai_Supabase__execute_sql` 도구 사용:
- query:

```sql
CREATE POLICY "storage_admin_insert" ON storage.objects FOR INSERT
  WITH CHECK (bucket_id = 'product-files' AND auth.role() = 'service_role');

CREATE POLICY "storage_admin_select" ON storage.objects FOR SELECT
  USING (bucket_id = 'product-files' AND auth.role() = 'service_role');
```

Expected: 성공

- [ ] **Step 3: Supabase 대시보드에서 Auth 공급자 활성화**

Supabase 대시보드 → Authentication → Providers:
- **Google**: Client ID, Client Secret 입력 후 활성화
- **Kakao**: Client ID, Client Secret 입력 후 활성화

Redirect URL 확인: `https://qisbzpglczxzfaaaoxxw.supabase.co/auth/v1/callback`

---

## Task 7: Supabase 클라이언트 유틸

**Files:**
- Create: `lib/supabase/server.ts`, `lib/supabase/client.ts`

- [ ] **Step 1: 서버 클라이언트 생성**

```typescript
// lib/supabase/server.ts
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'

export async function createClient() {
  const cookieStore = await cookies()
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return cookieStore.getAll()
        },
        setAll(cookiesToSet) {
          try {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options)
            )
          } catch {}
        },
      },
    }
  )
}

export async function createServiceClient() {
  const cookieStore = await cookies()
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    {
      cookies: {
        getAll() { return cookieStore.getAll() },
        setAll() {},
      },
    }
  )
}
```

- [ ] **Step 2: 브라우저 클라이언트 생성**

```typescript
// lib/supabase/client.ts
import { createBrowserClient } from '@supabase/ssr'

export function createClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  )
}
```

- [ ] **Step 3: 타입 체크**

```powershell
npx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 4: 커밋**

```powershell
git add lib/
git commit -m "feat: add supabase server and browser client utilities"
```

---

## Task 8: Middleware — 관리자 인가

**Files:**
- Create: `middleware.ts`

- [ ] **Step 1: middleware.ts 생성**

```typescript
// middleware.ts
import { createServerClient } from '@supabase/ssr'
import { NextResponse, type NextRequest } from 'next/server'

export async function middleware(request: NextRequest) {
  let supabaseResponse = NextResponse.next({ request })

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() { return request.cookies.getAll() },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value, options }) => {
            request.cookies.set(name, value)
            supabaseResponse = NextResponse.next({ request })
            supabaseResponse.cookies.set(name, value, options)
          })
        },
      },
    }
  )

  const { data: { user } } = await supabase.auth.getUser()

  const isAdminPath =
    request.nextUrl.pathname.startsWith('/admin') ||
    request.nextUrl.pathname.startsWith('/api/admin')

  if (isAdminPath) {
    if (!user) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
    }
    const { data: profile } = await supabase
      .from('profiles')
      .select('role')
      .eq('id', user.id)
      .single()

    if (profile?.role !== 'admin') {
      return NextResponse.json({ error: 'Forbidden' }, { status: 403 })
    }
  }

  return supabaseResponse
}

export const config = {
  matcher: ['/admin/:path*', '/api/admin/:path*'],
}
```

- [ ] **Step 2: 타입 체크**

```powershell
npx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 3: 커밋**

```powershell
git add middleware.ts
git commit -m "feat: add admin authorization middleware"
```

---

## Task 9: 인증 API — GET /api/auth/me

**Files:**
- Create: `app/api/auth/me/route.ts`

- [ ] **Step 1: route.ts 생성**

```typescript
// app/api/auth/me/route.ts
import { createClient } from '@/lib/supabase/server'
import { NextResponse } from 'next/server'

export async function GET() {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()

  if (!user) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
  }

  const { data: profile, error } = await supabase
    .from('profiles')
    .select('*')
    .eq('id', user.id)
    .single()

  if (error || !profile) {
    return NextResponse.json({ error: 'Profile not found' }, { status: 404 })
  }

  return NextResponse.json(profile)
}
```

- [ ] **Step 2: 타입 체크**

```powershell
npx tsc --noEmit
```

- [ ] **Step 3: 커밋**

```powershell
git add app/api/auth/
git commit -m "feat: add GET /api/auth/me"
```

---

## Task 10: 상품 API — GET /api/products, GET /api/products/[id]

**Files:**
- Create: `app/api/products/route.ts`, `app/api/products/[id]/route.ts`

- [ ] **Step 1: 상품 목록 API 생성**

```typescript
// app/api/products/route.ts
import { createClient } from '@/lib/supabase/server'
import { NextResponse } from 'next/server'

export async function GET() {
  const supabase = await createClient()

  const { data: products, error } = await supabase
    .from('products')
    .select('id, title, thumbnail_url, price, reviews(rating)')
    .eq('is_active', true)
    .order('created_at', { ascending: false })

  if (error) {
    return NextResponse.json({ error: error.message }, { status: 500 })
  }

  const result = (products ?? []).map((p) => {
    const ratings = (p.reviews as { rating: number }[])
    const rating = ratings.length > 0
      ? ratings.reduce((sum, r) => sum + r.rating, 0) / ratings.length
      : 0
    return {
      id: p.id,
      title: p.title,
      thumbnail_url: p.thumbnail_url,
      price: p.price,
      rating: Math.round(rating * 10) / 10,
      reviewCount: ratings.length,
    }
  })

  return NextResponse.json(result)
}
```

- [ ] **Step 2: 상품 상세 API 생성**

```typescript
// app/api/products/[id]/route.ts
import { createClient } from '@/lib/supabase/server'
import { NextResponse } from 'next/server'

export async function GET(
  _req: Request,
  { params }: { params: Promise<{ id: string }> }
) {
  const { id } = await params
  const supabase = await createClient()

  const { data: product, error } = await supabase
    .from('products')
    .select(`
      id, title, description, content, thumbnail_url, price, created_at,
      reviews (
        id, rating, content, created_at,
        profiles ( name, avatar_url )
      )
    `)
    .eq('id', id)
    .eq('is_active', true)
    .single()

  if (error || !product) {
    return NextResponse.json({ error: 'Not found' }, { status: 404 })
  }

  const reviews = product.reviews as Array<{
    id: string; rating: number; content: string | null; created_at: string;
    profiles: { name: string | null; avatar_url: string | null }
  }>
  const rating = reviews.length > 0
    ? Math.round((reviews.reduce((sum, r) => sum + r.rating, 0) / reviews.length) * 10) / 10
    : 0

  return NextResponse.json({ ...product, rating, reviewCount: reviews.length })
}
```

- [ ] **Step 3: 타입 체크**

```powershell
npx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 4: 커밋**

```powershell
git add app/api/products/
git commit -m "feat: add GET /api/products and GET /api/products/[id]"
```

---

## Task 11: 결제 유틸 + 이메일 유틸

**Files:**
- Create: `lib/portone.ts`, `lib/email.ts`

- [ ] **Step 1: Portone V2 검증 유틸 생성**

```typescript
// lib/portone.ts
interface PortonePayment {
  paymentId: string
  status: string
  amount: { total: number }
  orderName: string
}

export async function verifyPayment(
  paymentId: string,
  expectedAmount: number
): Promise<PortonePayment> {
  const response = await fetch(
    `https://api.portone.io/payments/${encodeURIComponent(paymentId)}`,
    {
      headers: {
        Authorization: `PortOne ${process.env.PORTONE_API_SECRET}`,
        'Content-Type': 'application/json',
      },
    }
  )

  if (!response.ok) {
    throw new Error(`Portone API 오류: ${response.status}`)
  }

  const payment: PortonePayment = await response.json()

  if (payment.status !== 'PAID') {
    throw new Error('결제가 완료되지 않았습니다')
  }

  if (payment.amount.total !== expectedAmount) {
    throw new Error('결제 금액이 주문 금액과 일치하지 않습니다')
  }

  return payment
}
```

- [ ] **Step 2: Resend 이메일 유틸 생성**

```typescript
// lib/email.ts
import { Resend } from 'resend'

const resend = new Resend(process.env.RESEND_API_KEY)

interface PurchaseEmailOptions {
  to: string
  productTitle: string
  amount: number
  orderId: string
}

export async function sendPurchaseConfirmation({
  to, productTitle, amount, orderId,
}: PurchaseEmailOptions): Promise<void> {
  const appUrl = process.env.NEXT_PUBLIC_APP_URL ?? 'http://localhost:3000'

  await resend.emails.send({
    from: 'vibe-store@resend.dev',
    to,
    subject: `[바이브 스토어] '${productTitle}' 구매가 완료되었습니다`,
    html: `
      <div style="font-family: sans-serif; max-width: 480px; margin: 0 auto;">
        <h2 style="color: #111827;">구매 완료</h2>
        <p>아래 상품의 결제가 정상적으로 완료되었습니다.</p>
        <table style="width: 100%; border-collapse: collapse; margin: 16px 0;">
          <tr><td style="padding: 8px 0; color: #6b7280;">상품명</td><td style="padding: 8px 0;">${productTitle}</td></tr>
          <tr><td style="padding: 8px 0; color: #6b7280;">결제 금액</td><td style="padding: 8px 0;">${amount.toLocaleString('ko-KR')}원</td></tr>
          <tr><td style="padding: 8px 0; color: #6b7280;">주문 번호</td><td style="padding: 8px 0; font-size: 12px; color: #9ca3af;">${orderId}</td></tr>
        </table>
        <a href="${appUrl}/mypage"
          style="display: inline-block; padding: 12px 24px; background: #2563EB; color: white; text-decoration: none; border-radius: 8px;">
          마이페이지에서 다운로드
        </a>
      </div>
    `,
  })
}
```

- [ ] **Step 3: 타입 체크**

```powershell
npx tsc --noEmit
```

- [ ] **Step 4: 커밋**

```powershell
git add lib/portone.ts lib/email.ts
git commit -m "feat: add Portone V2 verify util and Resend email util"
```

---

## Task 12: 결제 검증 API — POST /api/payments/verify

**Files:**
- Create: `app/api/payments/verify/route.ts`

- [ ] **Step 1: route.ts 생성**

```typescript
// app/api/payments/verify/route.ts
import { createClient, createServiceClient } from '@/lib/supabase/server'
import { verifyPayment } from '@/lib/portone'
import { sendPurchaseConfirmation } from '@/lib/email'
import { NextResponse } from 'next/server'

interface VerifyBody {
  paymentId: string
  orderId: string
}

export async function POST(req: Request) {
  const body: VerifyBody = await req.json()
  const { paymentId, orderId } = body

  if (!paymentId || !orderId) {
    return NextResponse.json({ error: 'paymentId와 orderId가 필요합니다' }, { status: 400 })
  }

  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })

  const { data: order } = await supabase
    .from('orders')
    .select('*, products(title), profiles(email)')
    .eq('id', orderId)
    .eq('user_id', user.id)
    .single()

  if (!order) return NextResponse.json({ error: '주문을 찾을 수 없습니다' }, { status: 404 })
  if (order.status === 'paid') return NextResponse.json({ success: true })

  try {
    await verifyPayment(paymentId, order.amount)

    const serviceClient = await createServiceClient()
    await serviceClient
      .from('orders')
      .update({ status: 'paid', payment_id: paymentId })
      .eq('id', orderId)

    const product = order.products as { title: string }
    const profile = order.profiles as { email: string }

    await sendPurchaseConfirmation({
      to: profile.email,
      productTitle: product.title,
      amount: order.amount,
      orderId,
    })

    return NextResponse.json({ success: true })
  } catch (err) {
    const serviceClient = await createServiceClient()
    await serviceClient
      .from('orders')
      .update({ status: 'failed' })
      .eq('id', orderId)

    return NextResponse.json(
      { error: (err as Error).message },
      { status: 400 }
    )
  }
}
```

- [ ] **Step 2: 타입 체크**

```powershell
npx tsc --noEmit
```

- [ ] **Step 3: 커밋**

```powershell
git add app/api/payments/
git commit -m "feat: add POST /api/payments/verify with Portone V2 and email"
```

---

## Task 13: 주문 API + 다운로드 API

**Files:**
- Create: `app/api/orders/my/route.ts`, `app/api/products/[id]/download/route.ts`

- [ ] **Step 1: 주문 내역 API 생성**

```typescript
// app/api/orders/my/route.ts
import { createClient } from '@/lib/supabase/server'
import { NextResponse } from 'next/server'

export async function GET() {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })

  const { data: orders, error } = await supabase
    .from('orders')
    .select('*, products(id, title, thumbnail_url, file_name)')
    .eq('user_id', user.id)
    .order('created_at', { ascending: false })

  if (error) return NextResponse.json({ error: error.message }, { status: 500 })

  return NextResponse.json(orders ?? [])
}
```

- [ ] **Step 2: 보안 다운로드 API 생성**

```typescript
// app/api/products/[id]/download/route.ts
import { createClient, createServiceClient } from '@/lib/supabase/server'
import { NextResponse } from 'next/server'

export async function GET(
  _req: Request,
  { params }: { params: Promise<{ id: string }> }
) {
  const { id } = await params
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })

  const { data: order } = await supabase
    .from('orders')
    .select('id')
    .eq('user_id', user.id)
    .eq('product_id', id)
    .eq('status', 'paid')
    .maybeSingle()

  if (!order) {
    return NextResponse.json({ error: '구매 내역이 없습니다' }, { status: 403 })
  }

  const serviceClient = await createServiceClient()

  const { data: product } = await serviceClient
    .from('products')
    .select('file_path, file_name')
    .eq('id', id)
    .single()

  if (!product?.file_path) {
    return NextResponse.json({ error: '파일을 찾을 수 없습니다' }, { status: 404 })
  }

  const { data: signedData, error: signError } = await serviceClient.storage
    .from('product-files')
    .createSignedUrl(product.file_path, 60, {
      download: product.file_name ?? true,
    })

  if (signError || !signedData) {
    return NextResponse.json({ error: '다운로드 URL 생성에 실패했습니다' }, { status: 500 })
  }

  return NextResponse.json({ url: signedData.signedUrl })
}
```

- [ ] **Step 3: 타입 체크**

```powershell
npx tsc --noEmit
```

- [ ] **Step 4: 커밋**

```powershell
git add app/api/orders/ app/api/products/
git commit -m "feat: add GET /api/orders/my and GET /api/products/[id]/download"
```

---

## Task 14: 관리자 상품 API

**Files:**
- Create: `app/api/admin/products/route.ts`, `app/api/admin/products/[id]/route.ts`

- [ ] **Step 1: 관리자 상품 등록 API 생성**

```typescript
// app/api/admin/products/route.ts
import { createServiceClient } from '@/lib/supabase/server'
import { NextResponse } from 'next/server'

const MAX_FILE_SIZE = 50 * 1024 * 1024 // 50MB

export async function POST(req: Request) {
  const formData = await req.formData()

  const title = formData.get('title') as string
  const description = formData.get('description') as string | null
  const content = formData.get('content') as string | null
  const price = Number(formData.get('price'))
  const thumbnailUrl = formData.get('thumbnail_url') as string | null
  const file = formData.get('file') as File | null

  if (!title || isNaN(price) || price < 0) {
    return NextResponse.json({ error: '필수 항목(title, price)을 확인하세요' }, { status: 400 })
  }

  const supabase = await createServiceClient()

  let filePath: string | null = null
  let fileName: string | null = null

  if (file) {
    if (file.size > MAX_FILE_SIZE) {
      return NextResponse.json({ error: '파일 크기는 50MB를 초과할 수 없습니다' }, { status: 400 })
    }
    fileName = file.name
    filePath = `${crypto.randomUUID()}/${fileName}`
    const { error: uploadError } = await supabase.storage
      .from('product-files')
      .upload(filePath, file)
    if (uploadError) {
      return NextResponse.json({ error: `파일 업로드 실패: ${uploadError.message}` }, { status: 500 })
    }
  }

  const { data, error } = await supabase
    .from('products')
    .insert({ title, description, content, price, thumbnail_url: thumbnailUrl, file_path: filePath, file_name: fileName })
    .select()
    .single()

  if (error) return NextResponse.json({ error: error.message }, { status: 500 })

  return NextResponse.json(data, { status: 201 })
}
```

- [ ] **Step 2: 관리자 상품 수정/삭제 API 생성**

```typescript
// app/api/admin/products/[id]/route.ts
import { createServiceClient } from '@/lib/supabase/server'
import { NextResponse } from 'next/server'

export async function PUT(
  req: Request,
  { params }: { params: Promise<{ id: string }> }
) {
  const { id } = await params
  const body = await req.json()
  const supabase = await createServiceClient()

  const allowed = ['title', 'description', 'content', 'price', 'thumbnail_url', 'is_active']
  const updates = Object.fromEntries(
    Object.entries(body).filter(([key]) => allowed.includes(key))
  )

  const { data, error } = await supabase
    .from('products')
    .update(updates)
    .eq('id', id)
    .select()
    .single()

  if (error) return NextResponse.json({ error: error.message }, { status: 500 })

  return NextResponse.json(data)
}

export async function DELETE(
  _req: Request,
  { params }: { params: Promise<{ id: string }> }
) {
  const { id } = await params
  const supabase = await createServiceClient()

  const { error } = await supabase
    .from('products')
    .update({ is_active: false })
    .eq('id', id)

  if (error) return NextResponse.json({ error: error.message }, { status: 500 })

  return NextResponse.json({ success: true })
}
```

- [ ] **Step 3: 타입 체크**

```powershell
npx tsc --noEmit
```

- [ ] **Step 4: 커밋**

```powershell
git add app/api/admin/products/
git commit -m "feat: add admin product CRUD API (POST/PUT/DELETE)"
```

---

## Task 15: 관리자 대시보드 API — GET /api/admin/dashboard

**Files:**
- Create: `app/api/admin/dashboard/route.ts`

- [ ] **Step 1: 대시보드 API 생성**

```typescript
// app/api/admin/dashboard/route.ts
import { createServiceClient } from '@/lib/supabase/server'
import { NextResponse } from 'next/server'

export async function GET() {
  const supabase = await createServiceClient()

  const today = new Date()
  today.setHours(0, 0, 0, 0)

  const thirtyDaysAgo = new Date()
  thirtyDaysAgo.setDate(thirtyDaysAgo.getDate() - 30)

  const [totalResult, todayResult, chartResult] = await Promise.all([
    supabase.from('orders').select('amount').eq('status', 'paid'),
    supabase.from('orders').select('id').eq('status', 'paid').gte('created_at', today.toISOString()),
    supabase.from('orders').select('amount, created_at').eq('status', 'paid').gte('created_at', thirtyDaysAgo.toISOString()),
  ])

  const totalSales = (totalResult.data ?? []).reduce((sum, o) => sum + o.amount, 0)
  const todaySalesCount = todayResult.data?.length ?? 0

  const salesChart = (chartResult.data ?? []).reduce<Record<string, number>>((acc, o) => {
    const date = o.created_at.slice(0, 10)
    acc[date] = (acc[date] ?? 0) + o.amount
    return acc
  }, {})

  return NextResponse.json({ totalSales, todaySalesCount, salesChart })
}
```

- [ ] **Step 2: 타입 체크**

```powershell
npx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 3: 개발 서버로 최종 확인**

```powershell
npm run dev
```

브라우저에서 `http://localhost:3000/api/products` 접속 → `[]` 또는 빈 배열 응답 확인 (DB가 비어 있으므로 정상)

`Ctrl+C`로 종료

- [ ] **Step 4: 최종 커밋 및 푸시**

```powershell
git add app/api/admin/dashboard/
git commit -m "feat: add GET /api/admin/dashboard"
git push
```

---

## 완료 체크리스트

- [ ] `GET /api/auth/me` — 로그인 유저 프로필 반환
- [ ] `GET /api/products` — 활성 상품 목록 (rating 포함)
- [ ] `GET /api/products/[id]` — 상품 상세 + 리뷰
- [ ] `POST /api/payments/verify` — Portone V2 검증 + 이메일 발송
- [ ] `GET /api/orders/my` — 내 구매 내역
- [ ] `GET /api/products/[id]/download` — 결제 검증 후 Signed URL 발급
- [ ] `POST /api/admin/products` — 상품 등록 (파일 업로드)
- [ ] `PUT /api/admin/products/[id]` — 상품 수정
- [ ] `DELETE /api/admin/products/[id]` — 상품 비활성화
- [ ] `GET /api/admin/dashboard` — 매출 통계
- [ ] Middleware — `/admin/*`, `/api/admin/*` 인가 차단
- [ ] DB 마이그레이션 적용 (테이블 4개 + RLS)
- [ ] Storage 버킷 설정 (비공개, 50MB 제한)
